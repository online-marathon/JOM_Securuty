spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MSSQLServer
    username: sa
    password:
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          import_files: ""

token:
  signing:
    key: testveryWellProtectedJwtSigningKey315553242142432531312